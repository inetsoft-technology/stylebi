---
description: Show login state and any active connected sheet session.
---

Call the MCP tool `status` with no arguments. Print the returned `summary` verbatim. No interpretation needed — it's already user-facing text.
